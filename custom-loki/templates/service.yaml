apiVersion: v1
kind: Service
metadata:
  name: {{ include "custom-loki.fullname" . }}
  labels:
    {{- include "custom-loki.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "custom-loki.selectorLabels" . | nindent 4 }}
